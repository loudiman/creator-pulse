---
phase: 03-collector-core-api-sources
plan: 06
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/creatorpulse/sources/_retry.py
  - src/creatorpulse/sources/youtube.py
  - tests/test_sources.py
autonomous: true
requirements: [SRC-05, OPS-06]

# Split out of `03-03-PLAN.md` on 2026-08-05, with the author's approval. 03-03 is deferred on
# SRC-02 (Twitch 2FA — no client id, so the five Twitch fixtures cannot be recorded), but its
# Task 1 — the retry decorator and the YouTube OPS-06 cases — needs no Twitch credential and no
# Twitch fixture. It is carried here whole rather than left to sit behind a wall it does not
# depend on. SRC-05 and OPS-06's YouTube half move here; 03-03 keeps SRC-02, SRC-04, and OPS-06's
# Twitch half. Nothing was weakened, narrowed, or dropped in the move.

must_haves:
  truths:
    - "A transient failure — `requests.Timeout`, `requests.ConnectionError`, HTTP 429, or any HTTP 5xx — is retried up to three attempts total with a fixed 2s then 4s sleep before the pair is recorded as failed (SRC-05, D-13, D-14)"
    - "Every other HTTP status fails on the first attempt: a 401 from a bad key, a 404 from a bad path, and YouTube's 403 `quotaExceeded` all surface in the journal within a second rather than after seven (D-13, D-14)"
    - "The retry log line names the identifier, the source, and the attempt number, and carries no request headers, no params dict, no client secret, and no bearer value"
    - "The hidden-subscriber fixture and a genuine zero-subscriber response produce different records — `None` versus `0` — proving the two cases never merge (OPS-06 adjacency)"
    - "Every source test asserts on a named `MetricRecord` field, never on tuple position, so reordering fields in `models.py` cannot make a passing test silently wrong (OPS-06 ordering)"
  prohibitions:
    - statement: "MUST NOT add a dependency for retry, backoff, or HTTP mocking — no retry library, no mocking library. Stdlib `time.sleep` and `unittest.mock` only."
    - statement: "MUST NOT widen the retryable set beyond D-13's four shapes. Retrying every `requests` exception class makes a configuration error look like a network problem in the journal for seven seconds before failing anyway, which is the failure mode the narrow list exists to prevent."
    - statement: "MUST NOT put retry logic, HTTP status handling, or any per-source failure taxonomy into `collector.py`. The wrapper lives in the source layer and nowhere else (ARCHITECTURE.md Anti-Pattern 1, D-13)."
    - statement: "MUST NOT substitute zero for a `None` metric, or a `None` for a real zero, anywhere between the HTTP response and the record. NULL means the platform does not expose the metric; 0 means the platform reported zero."
    - statement: "MUST NOT weaken, narrow, delete, or skip a test to reach green; the code is wrong until proven otherwise, and any test-file change during a Verify step is suspect unless the author asked for it."
    - statement: "MUST NOT make a live network call from inside the test suite. The retry cases are driven by a scripted local callable and a faked clock; the YouTube cases read the fixtures 03-01 recorded."
  artifacts:
    - path: "src/creatorpulse/sources/_retry.py"
      provides: "retry() — narrow exception list, fixed backoff, source layer only"
      contains: "def retry("
    - path: "src/creatorpulse/sources/youtube.py"
      provides: "the single existing requests.get call site, now wrapped — signature and parse unchanged"
      contains: "retry("
    - path: "tests/test_sources.py"
      provides: "the retry cases against a faked clock, plus YouTube fixture-in / record-out coverage"
      min_lines: 90
  key_links:
    - from: "src/creatorpulse/sources/youtube.py"
      to: "src/creatorpulse/sources/_retry.py"
      via: "the one outbound call goes through retry(); nothing else in the codebase imports it"
      pattern: "retry\\("
---

> **Forward reference, recorded rather than hidden.** `retry()`'s `creator_id` parameter accepts the
> sentinel `"_token"` for a call that happens before any creator-specific work. Nothing passes that
> sentinel until `03-03` executes and `sources/twitch.py`'s token mint exists. It is a documented
> forward reference to a deferred plan, **not dead code to be deleted** — the parameter is a plain
> string either way and the sentinel costs one sentence of docstring. Do not "clean it up" on the
> grounds that nothing uses it yet.

## Phase Goal

> Real numbers from YouTube and Twitch land in SQLite with history, and one broken source cannot
> take the run down.

*(ROADMAP `**Mode:** mvp` is set; the Phase 3 `**Goal:**` line is not in user-story form and is
reproduced verbatim rather than invented — see the planner's return note.)*

<objective>
The half of the phase's second wave that needs no credential: transient errors get three attempts,
everything else fails in one second, and the YouTube normalization cases land against the fixtures
03-01 recorded.

**This plan was split out of `03-03-PLAN.md` on 2026-08-05.** 03-03 is deferred on SRC-02 — a Twitch
application cannot be registered because 2FA enrolment needs a mobile number and the verification SMS
does not arrive — but the retry decorator was never a Twitch feature. It is the transient-error policy
*both* sources need, it wraps a YouTube call site that already exists, and its tests drive a scripted
local callable rather than any recorded fixture. Leaving it behind a wall it does not depend on would
have deferred SRC-05 for no reason.

Purpose: SRC-05 is one of the two requirements the tracer deliberately did not claim. This plan claims
it, and it lands OPS-06's YouTube half — the fixture-in / record-out proof that a hidden subscriber
count and a real zero produce different records.

Output: `sources/_retry.py`, the wrapper applied to the existing YouTube call site, and
`tests/test_sources.py`. `03-03` later *extends* that test file with the Twitch cases; it does not
create it.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/03-collector-core-api-sources/03-CONTEXT.md
@.planning/phases/03-collector-core-api-sources/03-RESEARCH.md
@.planning/phases/03-collector-core-api-sources/03-PATTERNS.md
@.planning/phases/03-collector-core-api-sources/03-VALIDATION.md
@.planning/phases/03-collector-core-api-sources/COVERAGE.md
@.planning/phases/03-collector-core-api-sources/03-02-SUMMARY.md
@.planning/research/PITFALLS.md
@src/creatorpulse/models.py
@src/creatorpulse/sources/__init__.py
@src/creatorpulse/sources/youtube.py
@tests/test_collector.py
</context>

## Interface contract (executors: build against this exact signature)

```python
# creatorpulse/sources/_retry.py                             NEW
def retry(
    fn: Callable[P, requests.Response],
    *,
    creator_id: str,          # the creators.yaml identifier, or the sentinel "_token"
    source: str,              # "youtube" | "twitch"
    max_attempts: int = 3,
) -> Callable[P, requests.Response]
```

`sources/youtube.py`'s `fetch(identifier, metric_date) -> MetricRecord` signature does **not** change.
`sources/__init__.py` does not change — no new registry entry lands in this plan.

Fixtures this plan's tests read, all recorded or derived by 03-01, all already on disk:

```
tests/fixtures/youtube/channel_ok.json                           # a normal channel, count public
tests/fixtures/youtube/channel_hidden_subs_derived.json          # flag true, subscriberCount "0"
tests/fixtures/youtube/channel_hidden_subs_omitted_derived.json  # flag true, key absent
```

<!-- planner-discipline-allow: tenacity -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Transient errors get three attempts; everything else fails in one second</name>
  <read_first>
    - `.planning/phases/03-collector-core-api-sources/03-RESEARCH.md` §"Retry decorator (D-13/D-14 — narrow list, fixed backoff, source layer only)" — the full verified block, including the comment explaining why a 403 returns on attempt 1
    - `.planning/phases/03-collector-core-api-sources/03-RESEARCH.md` §"Fixture-based test pattern for OPS-06" — the `Mock(spec=requests.Response)` shape verified live this session
    - `.planning/phases/03-collector-core-api-sources/03-CONTEXT.md` — D-13 (the narrow list, three attempts, 2s then 4s, one log line per retry), D-14 (fixed backoff only, and why the quota 403 must fail fast), D-03, D-18
    - `src/creatorpulse/sources/youtube.py` — the single `requests.get` call site this task wraps, and the `fetch` signature that must not change
    - `tests/test_collector.py` — the fixture-loading helper and the `Mock(spec=requests.Response)` factory 03-02 already wrote; reuse the shape, do not fork a second one
    - `pyproject.toml` — mypy `strict = true`; `requests` ships its own types and must stay strictly checked, so `ParamSpec`/`TypeVar` annotations are required rather than optional
  </read_first>
  <behavior>
    - A callable raising `requests.Timeout` on attempts 1 and 2 and succeeding on 3 returns the successful response; `time.sleep` was called with `2.0` then `4.0`.
    - A callable raising `requests.ConnectionError` on all three attempts re-raises the original exception after the third; `time.sleep` was called twice, never a third time.
    - A response with status 429 on attempt 1 and 200 on attempt 2 returns the 200; one retry line was logged.
    - A response with status 503 on all three attempts is returned as-is on the third, so the caller's `raise_for_status()` surfaces it.
    - A response with status 403 returns immediately on attempt 1; `time.sleep` was never called. This is YouTube's `quotaExceeded` shape, and the quota does not come back within seven seconds.
    - A response with status 401 or 404 behaves the same — one attempt, no sleep.
    - Every retry emits exactly one log line at INFO naming the identifier, the source, and the attempt number, and nothing else.
    - `youtube.fetch`'s externally visible behaviour is unchanged: the same records come out of the same fixtures, and the signature is byte-identical.
  </behavior>
  <files>src/creatorpulse/sources/_retry.py, src/creatorpulse/sources/youtube.py, tests/test_sources.py</files>
  <action>
Create `src/creatorpulse/sources/_retry.py` from RESEARCH.md's verified block. It lives in the source
layer and nowhere else — ARCHITECTURE.md Anti-Pattern 1 and D-13 both forbid the orchestrator knowing
about HTTP status codes or `requests` exception types, and the easiest way to violate that is to
"just add one small retry" in `collector.py` later. This module must import nothing from
`creatorpulse.sources`' own `__init__`, so the submodule import from a sibling source module resolves
cleanly while the package is still initialising.

`retry(fn, *, creator_id, source, max_attempts=3)` returns a wrapped callable with the same signature
(`ParamSpec` and a `Callable[P, requests.Response]` return type — mypy `strict = true` will not accept
a bare `Callable`). The retryable set is exactly D-13's narrow list and no wider: the exception tuple
is `(requests.Timeout, requests.ConnectionError)`, and the retryable statuses are `429` and anything
`>= 500`. Everything else returns on attempt 1. Retrying every exception class the HTTP library
defines was rejected because a configuration error would then look like a network problem in the
journal for seven seconds before failing anyway.

Backoff is fixed: `time.sleep(2.0 * attempt)`, giving 2s then 4s. The sleep is the same every time —
no variation between two runs at the same attempt number, and no reading of any rate-limit response
header (D-14). This job makes roughly thirteen API calls a day from one box
that nothing else shares, so header-aware waiting would be code that never runs; the documented
trigger to revisit is header handling becoming necessary, at which point a retry library is the
upgrade path. Do not add one now.

On the last attempt, a retryable *status* is returned rather than raised, so the caller's
`raise_for_status()` produces the real error; a retryable *exception* re-raises. Sleep only between
attempts, never after the last one — three attempts means two sleeps, seven seconds worst case, and a
test asserts the sleep count.

The log line is one `logger.info` per retry with lazy `%` arguments, naming the identifier, the
source, and the attempt number, and **nothing else**. It must never carry the request headers, the
params dict, the client secret, the API key, or a bearer value (T-03-04). Note deliberately, so
nobody "fixes" it: the parameter is called `creator_id` but a source module only ever holds the
`creators.yaml` *identifier*, not the creator's slug id — the identifier is how the config names that
creator to the source layer, and `collector.py`'s own failure line carries the real slug, so both are
greppable from one journal.

`creator_id` also accepts the sentinel `"_token"`, for a call that happens before any creator-specific
work. Document it in one docstring line. **Nothing passes it yet** — `03-03`'s deferred Twitch token
mint is its only consumer, and 03-03 is blocked on SRC-02. This is a recorded forward reference to a
plan that exists and is written, not speculative generality and not dead code: leave it in, do not
delete it, and do not build anything else around it.

Then apply the wrapper to `sources/youtube.py`'s single `requests.get` call site, passing the handle
as `creator_id` and `"youtube"` as `source`. Do not change `fetch`'s signature, its parse, or its two
raise conditions — 03-02 proved those and `tests/test_collector.py` asserts them.

Create `tests/test_sources.py` in `tests/test_config.py`'s style — module docstring restating the
fixtures-only rule, bare `def test_x(...) -> None:` functions, no classes. Cover every case in
`<behavior>`. Fake the clock with `monkeypatch.setattr("creatorpulse.sources._retry.time.sleep", ...)`
recording its arguments, so the suite proves the 2s-then-4s schedule without ever waiting seven
seconds. Drive the retry cases with a small stateful callable that returns a scripted sequence of
`Mock(spec=requests.Response)` objects or raises — no HTTP server, no new mocking library. Reuse the
fixture-loading helper and the response factory 03-02 already wrote rather than forking a second
copy. Add the YouTube OPS-06 cases here against `channel_ok.json`,
`channel_hidden_subs_derived.json`, and `channel_hidden_subs_omitted_derived.json` — assert
`followers is None` for both hidden shapes and an integer for the normal one, on named fields.

This file is created here and **extended** by `03-03` when SRC-02 unblocks; it is not created twice.
  </action>
  <verify>
    <automated>pytest tests/test_sources.py -x && pytest tests/test_collector.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_sources.py::test_retries_on_429_then_succeeds -x` exits 0
    - `pytest tests/test_sources.py::test_youtube_hidden_subscriber_count_maps_to_none -x` exits 0
    - `pytest tests/test_sources.py -x` exits 0 with at least 8 tests collected, and the whole file runs in under 5 seconds (proving the clock is faked, not waited on)
    - `pytest tests/test_collector.py -x` still exits 0 — wrapping the call site changed no observable YouTube behaviour
    - The retry list is narrow: `grep -c 'requests.Timeout' src/creatorpulse/sources/_retry.py` returns 1, `grep -c 'requests.ConnectionError' src/creatorpulse/sources/_retry.py` returns 1, and `grep -c 'RequestException' src/creatorpulse/sources/_retry.py` returns 0
    - Fixed backoff, no header parsing: `grep -Eci 'retry-after|ratelimit|jitter|random' src/creatorpulse/sources/_retry.py` returns 0
    - Retry lives in the source layer only: `grep -Ec 'retry|sleep|status_code|requests' src/creatorpulse/collector.py` returns 0
    - No secret in a log line: `grep -Ec 'logger\.[a-z]+\([^)]*(header|token|secret|key|params)' src/creatorpulse/sources/_retry.py` returns 0
    - Lazy logging only: `grep -Ec 'logger\.[a-z]+\(f"' src/creatorpulse/sources/_retry.py src/creatorpulse/sources/youtube.py` returns 0
    - No retry dependency added: `grep -c 'tenacity' pyproject.toml` returns 0 and `git diff --quiet HEAD -- pyproject.toml` exits 0
    - No HTTP mocking dependency: `grep -Ec 'responses|requests_mock|httpretty|vcr' tests/test_sources.py` returns 0
    - No live network call in the suite: `grep -Ec '^import requests$' tests/test_sources.py` may return 1 (for the `Mock(spec=...)` target) but `grep -Ec 'requests\.(get|post)\(' tests/test_sources.py` returns 0
    - The sentinel is documented and unconsumed: `grep -c '_token' src/creatorpulse/sources/_retry.py` returns at least 1, and `grep -rc '"_token"' src/creatorpulse/sources/youtube.py` returns 0 — YouTube never passes it, and `sources/twitch.py` does not exist yet
    - Nothing else moved: `git diff --name-only HEAD -- src/creatorpulse/sources/__init__.py src/creatorpulse/collector.py src/creatorpulse/db.py creators.yaml | wc -l` returns 0
    - Green gate: `ruff format --check .` exits 0 AND `ruff check .` exits 0 AND `mypy src/` exits 0 AND `pytest` exits 0
  </acceptance_criteria>
  <done>A blip gets three attempts and seven seconds; a bad key gets one attempt and one second; the difference is a fifteen-line function with no dependency behind it, and the schedule is proven by a faked clock rather than by a slow test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retry wrapper -> journald | The retry path logs on failure, which is precisely when a careless implementation dumps the request for debugging. |
| external HTTP response -> `MetricRecord` | Untrusted remote JSON is typed here. The YouTube half of the normalization boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Information Disclosure | `sources/_retry.py` log line | high | mitigate | The retry line carries identifier, source, and attempt number and nothing else — never the request headers, the params dict, the API key, or a bearer value. The YouTube key rides in the query string, so a line that logged the request would publish it to the journal. Enforced by the `logger\.[a-z]+\([^)]*(header\|token\|secret\|key\|params)` count-zero criterion. The Twitch mint half of this threat stays in `03-03`. Cross-ref `03-VALIDATION.md` row SRC-05, threat ref T-03-04. |
| T-03-03 | Tampering | retry log line carrying a `creators.yaml` identifier | low | mitigate | Lazy `%`-style logging — the identifier is an argument, never part of the format string, so it cannot forge a journal line. Enforced by the `logger\.[a-z]+\(f"` count-zero criterion. |
| T-03-SC | Tampering | npm/pip/cargo installs | low | mitigate | No packages installed; the two obvious temptations here (a retry library, an HTTP mocking library) are explicit prohibitions with count-zero grep gates. Enforced by `git diff --quiet HEAD -- pyproject.toml`. |
</threat_model>

## Flagged Assumptions (spec-less probe fallback)

No `03-SPEC.md` exists, so `## Edge Coverage` came from the deterministic fallback probe. **Owned by
this plan: 3** (SRC-05 ×1, OPS-06 ×2) — carried across from `03-03` in the 2026-08-05 split, not
newly derived. Two resolved to `covered` and are plain strings in `must_haves.truths`; one returned
`unclassified` and is surfaced here rather than auto-backstopped or dropped.

- **SRC-05 (probe: unclassified — review manually).** Manual read: SRC-05 says a transient failure
  retries with backoff *before the attempt is recorded as failed*. The retryable set, the attempt
  count, and the schedule are all tested. What stays unresolved is the classification itself —
  whether Twitch's and YouTube's real transient failures actually present as the four shapes D-13
  lists. A rate limit delivered as a 200 with an error object in the body, or a TLS reset surfacing
  as a `requests.SSLError` rather than a `ConnectionError`, would both fall outside the narrow list
  and fail on attempt 1. That is the *intended* trade — D-13 chose a narrow list precisely so a
  configuration error does not masquerade as a network problem — but it has not been observed against
  either live API, only reasoned about. Recorded as unresolved rather than assumed safe; the first
  real failure in the journal is what settles it.

Equality check: 3 owned = 2 covered + 1 surfaced as a flagged assumption. No silent drops.

**Phase-wide equality after the split:** 39 probe-surfaced edges = 32 covered truths + 1 backstop
truth + 6 flagged assumptions, across 03-02 (15), 03-03 (4), 03-04 (6), 03-05 (11), and 03-06 (3).
03-01 owns none. 03-03's original 7 became 4 + 3; the totals are unchanged, which is the point.

## Prohibition Recall (spec-less fallback, §B)

No SPEC `## Prohibitions` section exists, so recall ran in-prompt. Six kept items are authored
descriptor-less in `must_haves.prohibitions` above — the no-new-dependency rule, the narrow-list
rule, the layer rule, the NULL-versus-zero rule, the no-weakened-test rule, and the no-live-network
rule. Canon-referral breadcrumb-and-drop: credential exposure in logs is a canon security item
covered by T-03-04 and by `/gsd-secure-phase`, not minted here. Routine-engineering drops:
must-not-retry-forever, must-not-swallow-an-exception, and must-not-hardcode-a-timeout — owned by the
edge probe or by ordinary code review.

## Artifacts this phase produces

Every symbol, file, and behaviour Phase 3 creates. Nothing here exists at the start of the phase, so
none of it is drift. The same table appears in every plan in this phase — `03-02-PLAN.md` and
`03-04-PLAN.md` still introduce it as "all five plans" because they were deliberately not reopened
during the 2026-08-05 split that created this sixth plan.

| Kind | Name | Introduced by |
|------|------|---------------|
| dataclass | `creatorpulse.models.MetricRecord` | 03-02 |
| dataclass | `creatorpulse.models.RunResult` | 03-02 |
| constant | `creatorpulse.db.SCHEMA_DDL` | 03-02 |
| constant | `creatorpulse.db.UPSERT_METRIC` | 03-02 |
| function | `creatorpulse.db.connect(db_path, *, create) -> sqlite3.Connection` | 03-02 (`create=True`), 03-05 (`create=False`) |
| function | `creatorpulse.db.upsert_metric(conn, record) -> None` | 03-02 |
| function | `creatorpulse.db.write_run_row(conn, started_at, finished_at, rows_written, failure_count) -> None` | 03-02 |
| exception | `creatorpulse.db.DatabaseNotInitialized` | 03-05 |
| protocol | `creatorpulse.sources.SourceFetcher` | 03-02 |
| constant | `creatorpulse.sources.KNOWN_PLATFORMS` | 03-02 |
| constant | `creatorpulse.sources.FETCHERS` | 03-02 (youtube), 03-03 (twitch) |
| function | `creatorpulse.sources._retry.retry(fn, *, creator_id, source, max_attempts=3)` | 03-06 |
| function | `creatorpulse.sources.youtube.fetch(identifier, metric_date) -> MetricRecord` | 03-02 |
| exception | `creatorpulse.sources.youtube.ChannelNotFound` | 03-02 |
| function | `creatorpulse.sources.twitch.fetch(identifier, metric_date) -> MetricRecord` | 03-03 |
| exception | `creatorpulse.sources.twitch.ChannelNotFound` | 03-03 |
| function | `creatorpulse.config.validate(raw) -> None` | 03-04 |
| exception | `creatorpulse.config.ValidationError` (carries `.problems`) | 03-04 |
| function | `creatorpulse.collector.collect_once(conn, creators) -> RunResult` | 03-02 |
| DDL column | `metrics.video_count INTEGER` | 03-02 (D-01) |
| DDL column | `metrics.is_live INTEGER` | 03-02 (D-01) |
| table | `metrics`, `runs`; index `idx_metrics_creator_date` | 03-02 |
| CLI behaviour | `collect` opens the database, runs the collector, and returns 0 / 1 (failures) | 03-02 |
| CLI behaviour | `collect` exits 2 on a config validation failure, before any network call | 03-04 |
| CLI flag | `scripts/record_fixture.py --header` (repeatable) | 03-01 |
| file | `src/creatorpulse/models.py`, `db.py`, `collector.py`, `sources/__init__.py`, `sources/youtube.py` | 03-02 |
| file | `src/creatorpulse/sources/_retry.py` | 03-06 |
| file | `src/creatorpulse/sources/twitch.py` | 03-03 |
| file | `tests/test_db.py`, `tests/test_sources.py`, `tests/test_collector.py` | 03-05, 03-06, 03-02 (extended by 03-03) |
| file | four fixtures under `tests/fixtures/youtube/` — two recorded, two `*_derived.json` | 03-01 |
| file | five fixtures under `tests/fixtures/twitch/` — all recorded | 03-01 `## Deferred` (blocked, SRC-02) |
| file | `.planning/phases/03-collector-core-api-sources/03-UAT.md`, `COVERAGE.md` | 03-01, planning |
| removed | the engagement-rate column — never created, absent from the record and the DDL (D-02) | 03-02 |

<verification>
- `ruff format --check .`, `ruff check .`, `mypy src/`, and `pytest` all exit 0 — the Definition of Green
- `pytest tests/test_sources.py -x` exits 0 with at least 8 tests, running in under 5 seconds
- `pytest tests/test_collector.py -x` still exits 0 — the YouTube slice 03-02 proved is unchanged
- `sources/__init__.py`, `collector.py`, `db.py`, `creators.yaml`, and `pyproject.toml` are all unchanged by this plan
- The retry schedule is proven by a faked clock, and the narrow exception list is proven by grep
</verification>

<success_criteria>
- A transient blip retries three times over seven seconds; a bad key fails in one, and the difference is visible in the journal
- The retryable set is exactly D-13's four shapes and provably no wider
- The wrapper is imported by the source layer and by nothing else
- Both hidden-subscriber shapes map to `followers is None`, and a real zero stays a real zero
- `tests/test_sources.py` exists and is green, so `03-03` extends a working file rather than creating one
</success_criteria>

<output>
Create `.planning/phases/03-collector-core-api-sources/03-06-SUMMARY.md` when done
</output>
