---
phase: quick-260806-k5w
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/creatorpulse.service
  - deploy/creatorpulse.timer
  - deploy/creatorpulse-bot.service
  - .env.example
  - .claude/CLAUDE.md
  - .planning/phases/06-discord-bot/06-CONTEXT.md
autonomous: true
requirements: [BOT-06, OPS-01]

must_haves:
  truths:
    - "`git ls-files deploy/` lists three unit files; the Phase 2 D-12 claim that they are committed artifacts is true for the first time."
    - "`deploy/creatorpulse.service` and `deploy/creatorpulse.timer` match the author's droplet units character for character — they are transcriptions, not drafts."
    - "`deploy/creatorpulse-bot.service` exists as the second, diffable unit Phase 2 D-12 anticipated, and carries no config-path variable."
    - ".env.example names all four Phase 6 D-18 Discord variables, every value blank."
    - "`.claude/CLAUDE.md` Hard Rule 1 carries a dated amendment naming the clock as the reason, scoped to unit files only, and rules for SSH/user/UFW/docs plus Hard Rule 3 still read as binding."
    - "`06-CONTEXT.md` no longer contradicts itself: the ownership table and the scout-gap paragraph both point at D-21."
    - "The four-command gate is green and nothing was installed, enabled, or started."
  artifacts:
    - deploy/creatorpulse.service
    - deploy/creatorpulse.timer
    - deploy/creatorpulse-bot.service
  key_links:
    - "deploy/creatorpulse-bot.service ExecStart -> the `creatorpulse bot` console-script subcommand (still a stub returning 3 — see the do-not-enable warning)."
    - ".env.example DISCORD_CHANNEL_ID / DISCORD_GUILD_ID -> Phase 6 D-18/D-19's startup config validation."
    - ".claude/CLAUDE.md Hard Rule 1 amendment <-> 06-CONTEXT.md D-21 — each must name the other, or the exception is only half-recorded."
---

<objective>
Close three open items in one commit, all of them documentation-and-config, none of them code.

1. **Land the `deploy/` directory.** Phase 2 **D-12** states that `deploy/creatorpulse.service` and
   `deploy/creatorpulse.timer` are committed repository artifacts. They are not — `git ls-files` has
   no `deploy/` path and the units exist only in `/etc/systemd/system/` on the droplet. The author has
   supplied their verbatim contents; this plan transcribes them. It also lands the new
   `deploy/creatorpulse-bot.service` that Phase 6 **D-18**'s two-process split requires.
2. **Add the two missing Discord variable names to `.env.example`** per Phase 6 **D-18**, blank-valued.
3. **Record the Hard Rule 1 exception as a dated in-place amendment** in `.claude/CLAUDE.md`, and as
   **D-21** in `06-CONTEXT.md`, following the formatting precedent Phase 5 **D-02** established.

Purpose: the repo currently asserts three things that are false — that the units are committed, that
the ownership table forbids what the author has now permitted, and that the scout-noticed `deploy/`
gap cannot be closed by the agent. All three become true here.

Output: three new unit files, two new `.env.example` lines, two amended planning/governance documents.

**DO NOT install, enable, or start anything.** `creatorpulse bot` is still the stub at
`src/creatorpulse/cli.py:155` that logs and returns exit 3. `deploy/creatorpulse-bot.service` carries
`Restart=on-failure` with `RestartSec=10`, so enabling it now would put systemd into a permanent
ten-second restart loop against a command that is designed to fail. These files are landed to be
*diffable and reviewable*, not to be deployed. No `systemctl` command belongs in this plan.

**No Python source changes. No test changes.** If either appears in the diff, something has gone wrong.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/06-discord-bot/06-CONTEXT.md
</context>

<preflight_findings>
Verified against the repo at plan time — do not re-derive, and do not act on the stale version:

- **`CREATORPULSE_SHEETS_KEYFILE=` is already present** in the committed `.env.example`, directly
  under `CREATORPULSE_SHEET_ID=` where it belongs. Confirmed by `git show HEAD:.env.example` with a
  clean working tree. The Phase 4 drift was closed at some earlier point. **Only two lines are
  missing**, both Discord IDs. Do not add a duplicate `CREATORPULSE_SHEETS_KEYFILE` line.
- **`[tool.ruff]` in `pyproject.toml` sets only `target-version` and `line-length`**; there is no
  `include` widening. Ruff's default file selection is `.py`/`.pyi`/`.ipynb`, so a top-level `deploy/`
  of INI-syntax unit files cannot be reached by `ruff format --check .` or `ruff check .`. The gate is
  still run in Task 3 to prove it rather than assume it. **Contingency only, not a step:** if ruff
  does reach into `deploy/`, the fix is adding `extend-exclude = ["deploy"]` to `[tool.ruff]` and
  nothing else — never loosen `select`, never touch mypy or pytest configuration.
- **No `.gitattributes` exists.** Write the unit files with LF line endings.
- `.claude/CLAUDE.md` anchors: Hard Rule 1 begins at line 10; the existing rule-2 amendment heading is
  at line 19; the "Rules 1 and 3 are untouched and still bind in full." sentence is at line 31.
- `06-CONTEXT.md` anchors: the ownership table's unit-file row is line 31; the "Repo gap noticed
  during scout" paragraph is lines 44-49; `</decisions>` closes at line 312.
</preflight_findings>

<artifacts>
The exact bodies for every file this plan writes. Tasks reference these by id. Reproduce them
byte for byte: do not reformat, do not reorder keys, do not add comments, do not "improve" them.

### A1 — `deploy/creatorpulse.service` (16 lines)

Transcribed verbatim from the author's `systemctl cat` output. **Human-authored — transcription, not
generation.** Not a character may differ.

```ini
[Unit]
Description=CreatorPulse Collector Service
After=network.target

[Service]
Type=oneshot
User=creatorpulse
WorkingDirectory=/home/creatorpulse/creator-pulse
EnvironmentFile=/etc/creatorpulse/creatorpulse.env
Environment="HOME=/home/creatorpulse"
Environment="CREATORPULSE_CONFIG=/home/creatorpulse/creator-pulse/creators.yaml"
Environment="CREATORPULSE_DB=/var/lib/creatorpulse/creatorpulse.db"
ExecStart=/home/creatorpulse/creator-pulse/.venv/bin/creatorpulse collect

[Install]
WantedBy=multi-user.target
```

### A2 — `deploy/creatorpulse.timer` (9 lines)

Transcribed verbatim. **Human-authored.**

```ini
[Unit]
Description=CreatorPulse Daily Schedule (08:00 Asia/Manila)

[Timer]
OnCalendar=*-*-* 08:00:00 Asia/Manila
Persistent=true

[Install]
WantedBy=timers.target
```

### A3 — `deploy/creatorpulse-bot.service` (17 lines)

**New, agent-drafted under the 2026-08-06 exception, author-approved in conversation.** This is the
file the amendment exists for.

```ini
[Unit]
Description=CreatorPulse Discord Bot Service
After=network.target

[Service]
Type=simple
User=creatorpulse
WorkingDirectory=/home/creatorpulse/creator-pulse
EnvironmentFile=/etc/creatorpulse/creatorpulse.env
Environment="HOME=/home/creatorpulse"
Environment="CREATORPULSE_DB=/var/lib/creatorpulse/creatorpulse.db"
ExecStart=/home/creatorpulse/creator-pulse/.venv/bin/creatorpulse bot
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### A4 — the four deliberate differences between A3 and A1

Record these as prose in the SUMMARY. They are what the author has to be able to defend when asked why
there are two units instead of one, and they are exactly the diff Phase 2 D-12 wanted to be diffable:

- **`Type=simple`, not `oneshot`.** The bot is long-lived and never exits; `oneshot` would have systemd
  wait forever for a process that is working correctly.
- **`Restart=on-failure` with `RestartSec=10`.** A dead one-shot collector is yesterday's problem and
  the timer fires again tomorrow. A dead bot is a silent outage that nothing else notices.
- **`WantedBy=multi-user.target`, enabled directly — no `.timer` fires it.** It is always up. The
  collector is started by `creatorpulse.timer`; the bot is started by boot.
- **No config-path `Environment=` line.** The bot never reads `creators.yaml`. Phase 6 D-15 resolves
  `/creator <name>` against `creator_id` values already in the database. Two processes, one config
  file, and only the writer reads it.

### A5 — `.env.example`, the two lines to add

Both go inside the existing `# Discord bot + webhook` block, so that block reads, in this order:

```
DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=
DISCORD_GUILD_ID=
DISCORD_WEBHOOK_URL=
```

Values stay empty. This file is committed; the real values live only in
`/etc/creatorpulse/creatorpulse.env`, root-owned and `chmod 600` (Phase 2 D-04). Nothing else in the
file changes.

<!-- planner-discipline-allow: GOOGLE_SERVICE_ACCOUNT_FILE -->
**Do not add `GOOGLE_SERVICE_ACCOUNT_FILE`.** It was a stray droplet-only line, no code reads it, and
the author has already deleted it there. It was never in the committed template and must not be
introduced by this plan.

### A6 — `.claude/CLAUDE.md`, Hard Rule 1 amendment

Three edits, mirroring exactly how the existing `### Amendment 2026-08-06 — rule 2 relaxed to \`mixed\``
block is structured. Annotate the original rule text rather than deleting it.

**A6.1 — annotate rule 1 in place.** Rule 1 currently ends `...you may write Python the units invoke;
you may not write the units.` Append to that sentence, on the same list item, an italic or bold pointer
in the style rule 2 uses for its own annotation: a dated marker saying rule 1 was amended on
2026-08-06, narrowed to unit files, see below. Do **not** strike the rule through — unlike rule 2, only
one clause of it was relaxed, and the SSH/user/UFW clauses still bind exactly as written.

**A6.2 — patch the rule-2 amendment's closing sentence.** Line 31 currently reads
`**Rules 1 and 3 are untouched and still bind in full.**` That was true of the rule-2 amendment and is
now half-false of the document. Leave the sentence — it correctly scopes the *rule-2* exception — and
append a short parenthetical noting that rule 1's unit-file clause was later narrowed by the amendment
below, with rule 3 still untouched.

**A6.3 — add a new dated amendment subsection** after the rule-2 one, at the same heading level
(`###`), titled for the date and the change. It must state, in prose in the author's register:

- **What changed:** the agent may now write `.service` and `.timer` files into `deploy/`.
- **What did not:** SSH configuration, non-root user setup, UFW rules, and `docs/deploy.md` are
  untouched and bind in full. The exception is unit files, nothing else. Hard Rule 3 (Discord
  Developer Portal) is untouched; note that the author completed BOT-07 by hand on 2026-08-06 — bot
  registration, zero privileged intents, a non-admin role with View Channel and Send Messages only.
- **The reason, named as the reason:** roughly thirteen hours to the interview with Phases 6 and 7
  outstanding, and Phase 6 needs a second unit. Granted explicitly by the author in conversation.
  Write it as the clock, the way the rule-2 amendment does — not dressed up as a design improvement.
- **The control that replaces hand-writing:** ROADMAP Phase 6 criterion 5 stands unchanged — *"the bot
  survives a reboot as its own systemd service, independent of the collector, and the author can
  explain which intents it requests and why none of them are privileged."* It closes only when the
  author installs the unit themselves and writes that explanation into `06-UAT.md` in their own words,
  unaided. Same control structure as Phase 5 D-03.
- **The transcription distinction:** `deploy/creatorpulse.service` and `deploy/creatorpulse.timer` are
  transcriptions of the author's own hand-written files, not generated ones. Only
  `deploy/creatorpulse-bot.service` is agent-drafted, and it is the only file that needed this
  exception.
- **Scope statement and cross-reference:** one scoped, dated exception, not a general relaxation. See
  `.planning/phases/06-discord-bot/06-CONTEXT.md` D-21.

### A7 — `06-CONTEXT.md`, three edits

**A7.1 — append `D-21`** at the end of the `<decisions>` section, immediately before the closing
`</decisions>` tag, in the same style as D-01 through D-20: a bold decision statement, rationale,
rejected alternatives where any exist, and a closing `— **Reversibility:**` line. It records the same
amendment from the phase's side: the agent writes the bot's unit file into `deploy/`; the reason is the
clock and it is named as the clock; SSH/user/UFW/`docs/deploy.md` and Hard Rule 3 are untouched;
criterion 5's author-installs-and-explains control is what replaces hand-writing; the two collector
units are transcriptions and only the bot unit is agent-drafted. Cross-reference `.claude/CLAUDE.md`'s
2026-08-06 rule-1 amendment and Phase 2 D-12 (whose "including when asked" clause this narrows).

**A7.2 — correct the ownership table.** The row currently reading

> `| The bot's \`.service\` unit file | **human** | not write, not edit, not draft, not "fix" |`

must be updated to reflect the amendment, with a pointer to D-21, rather than leaving the file
asserting the opposite of what it now decides three sections later. Keep the table's shape and the
other three rows unchanged.

**A7.3 — rewrite the "Repo gap noticed during scout" paragraph.** It currently ends by saying the agent
cannot close the gap because Hard Rule 1 forbids it. That is now false for unit files. Rewrite it to say
the gap is being closed by this quick task, which lands all three units in `deploy/`, and that
`docs/deploy.md` remains outstanding and human-owned (Phase 2 D-13, untouched by the amendment). Keep
the paragraph's role — it is still a scout note about pre-existing Phase 2 debt, not a Phase 6 problem.
</artifacts>

<tasks>

<task type="tracer">
  <name>Task 1: Land deploy/ and the two Discord variable names</name>
  <files>deploy/creatorpulse.service, deploy/creatorpulse.timer, deploy/creatorpulse-bot.service, .env.example</files>
  <action>
Create the `deploy/` directory and write the three unit files exactly as given in artifacts A1, A2, and
A3. Reproduce each body character for character — same key order, same quoting, same blank-line
placement, no trailing whitespace, LF line endings, one trailing newline at end of file. A1 and A2 are
transcriptions of the author's own hand-written droplet units (Phase 2 D-12) and altering them would
make the repo copy a lie about what is running; A3 is the agent-drafted bot unit permitted by the
2026-08-06 amendment recorded in Task 2 (D-21).

These are plain files. Do not mark them executable and do not adjust their mode.

Then edit `.env.example` per artifact A5: add `DISCORD_CHANNEL_ID=` and `DISCORD_GUILD_ID=` inside the
existing `# Discord bot + webhook` block, between the two lines already there, so the four Phase 6 D-18
names sit together. Both values stay empty — this template is committed and no real secret may ever
enter it (Phase 1 / Phase 2 D-04). Change nothing else in the file: per the preflight findings the
Sheets keyfile line is already present, so adding it again would produce a duplicate. Read the file
before editing and confirm that for yourself.

Do not run any `systemctl` command. Nothing here is installed, enabled, or started.
  </action>
  <verify>
    <automated>test -f deploy/creatorpulse.service && test -f deploy/creatorpulse.timer && test -f deploy/creatorpulse-bot.service && [ "$(wc -l &lt; deploy/creatorpulse.service)" = "16" ] &amp;&amp; [ "$(wc -l &lt; deploy/creatorpulse.timer)" = "9" ] &amp;&amp; [ "$(wc -l &lt; deploy/creatorpulse-bot.service)" = "17" ] &amp;&amp; [ "$(grep -c 'Type=oneshot' deploy/creatorpulse.service)" = "1" ] &amp;&amp; [ "$(grep -c 'OnCalendar=\*-\*-\* 08:00:00 Asia/Manila' deploy/creatorpulse.timer)" = "1" ] &amp;&amp; [ "$(grep -c 'Type=simple' deploy/creatorpulse-bot.service)" = "1" ] &amp;&amp; [ "$(grep -c 'RestartSec=10' deploy/creatorpulse-bot.service)" = "1" ] &amp;&amp; [ "$(grep -c 'bin/creatorpulse bot' deploy/creatorpulse-bot.service)" = "1" ] &amp;&amp; [ "$(grep -c 'CREATORPULSE_CONFIG' deploy/creatorpulse-bot.service)" = "0" ] &amp;&amp; [ "$(grep -c '^DISCORD_CHANNEL_ID=$' .env.example)" = "1" ] &amp;&amp; [ "$(grep -c '^DISCORD_GUILD_ID=$' .env.example)" = "1" ] &amp;&amp; [ "$(grep -c '^CREATORPULSE_SHEETS_KEYFILE=$' .env.example)" = "1" ] &amp;&amp; [ "$(grep -c 'GOOGLE_SERVICE_ACCOUNT_FILE' .env.example)" = "0" ] &amp;&amp; [ "$(grep -cE '^[A-Z_]+=.+' .env.example)" = "0" ] &amp;&amp; echo TASK1_OK</automated>
  </verify>
  <done>
`deploy/` holds three unit files whose bodies match A1/A2/A3 exactly. The bot unit is `Type=simple`
with `Restart=on-failure`/`RestartSec=10` and carries no config-path variable. `.env.example` names all
four Phase 6 D-18 Discord variables plus the pre-existing Sheets pair, every value blank — the last
grep in the gate proves no variable in the file has a non-empty value. Nothing was installed or enabled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the Hard Rule 1 amendment in CLAUDE.md and as D-21</name>
  <files>.claude/CLAUDE.md, .planning/phases/06-discord-bot/06-CONTEXT.md</files>
  <reversibility rating="costly">A published, dated rule relaxation is awkward to rescind once the exception has already been used to land a file — reversing it would mean deleting an amendment that a committed artifact depends on for its legitimacy. Written narrowly and with an explicit non-license clause for exactly that reason.</reversibility>
  <action>
Read `.claude/CLAUDE.md`'s existing `### Amendment 2026-08-06 — rule 2 relaxed to \`mixed\`` block
first and mirror its structure, register, and length. It is the precedent this project set for how a
hard rule gets changed: annotated in place, dated, reason named plainly as the reason, control that
replaces the removed one stated explicitly, and a closing sentence saying what the exception does not
license. A rule that gets quietly violated and a rule that gets amended on the record are different
things, and only the second one survives being asked about.

Apply the three `.claude/CLAUDE.md` edits described in artifact A6 — the in-place annotation on rule 1
(A6.1), the parenthetical repair to the rule-2 amendment's now half-false closing sentence (A6.2), and
the new dated `###` amendment subsection (A6.3). Use scoped edits; do not rewrite the file.

Then apply the three `06-CONTEXT.md` edits described in artifact A7 — append D-21 before
`</decisions>` (A7.1), correct the ownership table's unit-file row (A7.2), and rewrite the scout-gap
paragraph (A7.3). After these edits the file must not assert both that the agent may not write the bot
unit and that it may; the table, the paragraph, and D-21 must all say the same thing.

Both documents must name each other. An exception recorded in only one of them is an exception the
next agent will read one side of.

Touch no Python and no test file.
  </action>
  <verify>
    <automated>[ "$(grep -c '^### Amendment 2026-08-06' .claude/CLAUDE.md)" = "2" ] &amp;&amp; [ "$(grep -c '06-CONTEXT.md. D-21\|06-CONTEXT.md` D-21' .claude/CLAUDE.md)" -ge "1" ] &amp;&amp; [ "$(grep -ci 'docs/deploy.md' .claude/CLAUDE.md)" -ge "1" ] &amp;&amp; [ "$(grep -c 'D-21' .planning/phases/06-discord-bot/06-CONTEXT.md)" -ge "3" ] &amp;&amp; [ "$(grep -c 'not write, not edit, not draft' .planning/phases/06-discord-bot/06-CONTEXT.md)" = "0" ] &amp;&amp; [ "$(grep -c 'the agent cannot close it' .planning/phases/06-discord-bot/06-CONTEXT.md)" = "0" ] &amp;&amp; [ "$(git diff --name-only -- src tests | wc -l)" = "0" ] &amp;&amp; echo TASK2_OK</automated>
    <human-check>Read the new amendment in `.claude/CLAUDE.md` and D-21 in `06-CONTEXT.md` end to end. Confirm they describe the permission you actually granted — unit files only, in `deploy/`, because of the clock — and that nothing in either one reads as a broader relaxation than you intended. This is your rule; the wording has to be yours to defend.</human-check>
  </verify>
  <done>
`.claude/CLAUDE.md` carries two dated 2026-08-06 amendments. Rule 1 is annotated in place rather than
deleted, its SSH/user/UFW clauses and `docs/deploy.md` still read as binding, Hard Rule 3 is explicitly
untouched with BOT-07's hand completion noted, the clock is named as the reason, criterion 5 is named
as the replacing control, and D-21 is cross-referenced. `06-CONTEXT.md` holds D-21 with a
`— **Reversibility:**` line, its ownership table row and scout-gap paragraph now agree with it, and the
two contradiction greps return zero. `git diff` shows no change under `src/` or `tests/`.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the four-command gate is still green</name>
  <files>pyproject.toml</files>
  <action>
Run the full Definition of Green in order: `ruff format --check .`, then `ruff check .`, then
`mypy src/`, then `pytest`. All four must pass. This plan added a new top-level directory of INI-syntax
files and changed a dotfile template, so the gate is being *proved* rather than assumed even though the
preflight finding says ruff's default file selection cannot reach `deploy/`.

Expected outcome: `pyproject.toml` is not modified and this task's diff is empty. That is the success
case, not a skipped task.

Only if `ruff format --check .` or `ruff check .` reports a finding inside `deploy/`: add
`extend-exclude = ["deploy"]` to the existing `[tool.ruff]` table in `pyproject.toml` and re-run. That
is the entire permitted fix. Do not touch `[tool.ruff.lint] select`, do not add a mypy override, do not
change pytest configuration, and do not weaken or edit any test — a test-file change during a verify
step is suspect unless the author asked for it, and the author did not.

If any of the four fails for a reason that is not ruff reaching into `deploy/`, stop and report it
rather than fixing it here. Nothing in this plan can plausibly break mypy or pytest, so such a failure
is either pre-existing or evidence that Task 1 or Task 2 touched something it should not have.
  </action>
  <verify>
    <automated>ruff format --check . &amp;&amp; ruff check . &amp;&amp; mypy src/ &amp;&amp; pytest -q &amp;&amp; echo GATE_GREEN</automated>
  </verify>
  <done>
All four commands exit 0. `git status` shows changes confined to `deploy/`, `.env.example`,
`.claude/CLAUDE.md`, and `.planning/`. No unit was installed, enabled, or started, and
`creatorpulse bot` remains the stub it was.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → git history | Anything committed here is permanent and public-shaped; a secret committed is a rotation, not a revert. |
| repo `deploy/` copy → droplet `/etc/systemd/system/` | The repo copy is documentation of what runs; drift between them makes the committed file misleading rather than dangerous. |
| governance docs → future agents | `.claude/CLAUDE.md` and `06-CONTEXT.md` are read as authority by every later agent; an over-broad amendment silently widens what they will do. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-k5w-01 | Information Disclosure | `.env.example` | critical | mitigate | Every value stays empty. Task 1's gate asserts `grep -cE '^[A-Z_]+=.+'` equals 0 across the whole file, so a non-empty value on any variable — not just the two added — fails the task. Phase 2 D-04's real-file-is-root-owned-600 rule is untouched. |
| T-k5w-02 | Information Disclosure | `deploy/*.service` | high | mitigate | Units carry no secrets by construction (Phase 2 D-12): they point `EnvironmentFile` at `/etc/creatorpulse/creatorpulse.env` and name only paths. Artifacts A1/A3 contain no credential. Verified by reading the bodies before commit — there is nothing to redact and redacting would strip the reviewable detail. |
| T-k5w-03 | Denial of Service | `deploy/creatorpulse-bot.service` | medium | mitigate | `Restart=on-failure` + `RestartSec=10` against the exit-3 stub would loop forever. Mitigated by scope: this plan lands the file only. The do-not-enable warning is stated in `<objective>`, restated in Task 1's action, and Task 3's `<done>` asserts nothing was enabled. Phase 6 owns the install, after the stub is filled. |
| T-k5w-04 | Elevation of Privilege | `.claude/CLAUDE.md` Hard Rule 1 | high | mitigate | An amendment written loosely would license future agents to write SSH config, UFW rules, and `docs/deploy.md`. A6.3 requires an explicit non-license clause naming each of those as still binding; Task 2's gate greps for `docs/deploy.md` in the file and the human-check puts the final wording in front of the author. Hard Rule 3 restated as untouched. |
| T-k5w-05 | Repudiation | `06-CONTEXT.md` | medium | mitigate | A rule relaxed in one document but denied in another leaves no coherent record of what was permitted or why. A7.2/A7.3 remove both contradictions and Task 2's gate asserts the two stale assertions are gone. Both documents cross-reference each other. |
| T-k5w-06 | Tampering | test suite | high | mitigate | Task 3 forbids any test edit and Task 2's gate asserts `git diff --name-only -- src tests` is empty. The four-command gate is proved, not asserted. |
| T-k5w-SC | Tampering | npm/pip/cargo installs | high | accept | No package install of any kind occurs in this plan — no dependency is added, removed, or upgraded, and `pyproject.toml` changes only in the contingency branch of Task 3 and only by an `extend-exclude` key. The Package Legitimacy Gate has no surface here. |
</threat_model>

<verification>
1. `git ls-files deploy/` lists exactly three paths after staging — Phase 2 D-12's claim becomes true.
2. The bodies of `deploy/creatorpulse.service` and `deploy/creatorpulse.timer` match the author's
   droplet units character for character. If the author still has an SSH session, the honest check is
   `systemctl cat creatorpulse.service` diffed against the repo copy; if not, the line-count and
   anchored greps in Task 1 stand as the automated proof.
3. `.env.example` names all four Phase 6 D-18 Discord variables and no variable in the file carries a
   value.
4. `.claude/CLAUDE.md` has two dated 2026-08-06 amendments and no sentence that contradicts either.
5. `06-CONTEXT.md`'s ownership table, scout-gap paragraph, and D-21 all agree.
6. `ruff format --check .`, `ruff check .`, `mypy src/`, `pytest` all pass.
7. `git status` shows no change under `src/` or `tests/`.
8. No `systemctl` command was run.
</verification>

<success_criteria>
- Three unit files committed under `deploy/`, byte-identical to artifacts A1, A2, A3.
- `.env.example` gains exactly two lines, both blank-valued Discord IDs.
- The Hard Rule 1 exception is recorded in both `.claude/CLAUDE.md` and `06-CONTEXT.md` D-21, dated
  2026-08-06, scoped to unit files, naming the clock as the reason and criterion 5 as the control.
- No self-contradiction remains in `06-CONTEXT.md`.
- Four-command gate green; zero changes under `src/` or `tests/`.
- Nothing installed, enabled, or started.
</success_criteria>

<output>
Create `.planning/quick/260806-k5w-land-deploy-unit-files-env-example-disco/260806-k5w-SUMMARY.md` when done.

Record in it: the A4 four-differences prose (the author needs it out loud), the fact that A1/A2 are
transcriptions while only A3 is agent-drafted, and the standing warning that
`deploy/creatorpulse-bot.service` must not be enabled until `creatorpulse bot` is more than the
exit-3 stub.
</output>
